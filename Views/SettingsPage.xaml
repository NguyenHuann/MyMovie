<Page
    x:Class="MyMovie.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="40,20,40,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Cài đặt" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,32" />

        <ScrollViewer Grid.Row="1">
            <StackPanel Spacing="24" MaxWidth="800" HorizontalAlignment="Left">

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="24"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel Spacing="16">
                        <TextBlock Text="Cá nhân hóa" FontWeight="Bold" FontSize="16"/>

                        <StackPanel Spacing="8">
                            <TextBlock Text="Tên người dùng" />
                            <TextBox x:Name="UsernameTextBox" Width="300" HorizontalAlignment="Left" PlaceholderText="Nhập tên của bạn..."/>
                            <Button x:Name="SaveNameButton" Content="Lưu thay đổi" Style="{StaticResource AccentButtonStyle}" Click="SaveNameButton_Click" Margin="0,8,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="24"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Thông tin ứng dụng" FontWeight="Bold" FontSize="16"/>
                        <TextBlock Text="Phiên bản: 1.0.0 (BETA)" Opacity="0.8"/>
                        <TextBlock Text="Nền tảng: .NET 8.0 + WinUI 3" Opacity="0.8"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>